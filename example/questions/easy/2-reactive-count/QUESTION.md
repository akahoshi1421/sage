# ref でカウンターを作る

`ref` を使ってカウンターを作ってください。

## 要件

1. `count` という名前のリアクティブな値を `0` で初期化する
2. `increment` 関数を呼ぶと `count` が 1 増える
3. ボタンをクリックすると `increment` が呼ばれ、ボタンに現在の `count` が表示される

> **メモ**: `ref` で作った値は `<script setup>` の中では `.value` でアクセスします。
